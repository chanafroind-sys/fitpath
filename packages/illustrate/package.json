{
  "name": "@fitpath/illustrate",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Draw an @fitpath/engine item as an SVG, from its box model and nothing else. Pure TypeScript, no DOM, runs at build time.",
  "license": "MIT",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@fitpath/engine": "*"
  }
}
