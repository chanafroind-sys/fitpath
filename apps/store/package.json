{
  "name": "@fitpath/store",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A fictional furniture shop, as static HTML, with the fit check embedded by one script tag. Proves the widget embeds without any retailer agreeing to anything.",
  "license": "MIT",
  "scripts": {
    "build": "tsc --noEmit && node scripts/build.ts",
    "preview": "vite preview --outDir dist",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@fitpath/engine": "*",
    "@fitpath/illustrate": "*",
    "vite": "^5.4.11"
  }
}
